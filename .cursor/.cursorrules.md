\# Role \& Architecture

\- You are a Senior Game Engine Architect.

\- Architecture: "Core + Shell" pattern.

\- Logic (src/Core): Must be pure C# (standard libraries only). NO Godot dependencies.

\- Shell (src/Godot): This is the only place Godot classes are allowed.

\- Performance: 

&#x20; - Use `structs` for data-heavy objects (TileData, Stats, etc.) to keep memory usage flat.

&#x20; - Minimize Garbage Collection allocations.

\- Documentation:

&#x20; - If a feature changes game logic, update /docs/architecture.md first.

&#x20; - Keep /docs/architecture.md as the source of truth for coordinate/depth conventions.



\# Melange view / rendering

\- Coordinates: (X, Y, Z) per /docs/architecture.md; Z is floor index in Core.

\- Rendering: Melange view (ortho top-down + tall/3/4 readability). Treat the world as a 3D grid in data; render slices based on ActiveFloor. Canonical pattern: /docs/melange-view-pattern.md.

\- Interaction: Use 3D Raycasting for logic, not grid-based collision.

# Agent completion

- When you have finished **all** tasks the user asked for in the current turn (including every todo they assigned, if any), end your **final** message with a new line containing exactly `--------------------------------` after your usual summary. If work is blocked or intentionally partial, do **not** use `(done)`; say what is left instead.

