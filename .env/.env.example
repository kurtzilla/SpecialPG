# Copy to a new file under .env/ (e.g. .env.local) for machine-specific values.
# All files in .env/ except .env.example are gitignored.

# Optional: override paths used by tools or CI (examples only; adjust names to match your usage)
# SPECIALPG_DATA_ROOT=D:\SpecialPGData
# GODOT_EXPORT_PATH=
